using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Dead class for now, might get repurposed later.
public class TargetLockButton : MonoBehaviour
{
//    public void SelectAsTarget()
//    {
//        foreach(EnemyCard card in Combat.Instance.enemyCards)
//        {
//            card.isTarget = false;
//        }

//        EnemyCard eCard = transform.parent.parent.parent.GetComponent<EnemyCard>();
//        eCard.isTarget = true;
//        Combat.Instance.currentSelectedTarget = eCard;
//    }
}
